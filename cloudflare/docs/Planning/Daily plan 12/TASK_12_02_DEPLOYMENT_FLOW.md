# TASK 12-02: GitHub Deployment (Apply Flow)

## Objective
Implement the logic to create branches, commit files, and open PRs via GitHub.

## Requirements
- Create a new feature branch for each generation.
- Commit generated files to the new branch.
- Open a Pull Request automatically.
- Handle API errors and retries.

## Deliverables
- Updated `builder.apply()` logic in `platform-api`.
- Successful end-to-end deployment from UI to GitHub.
