# TASK 12-01: HTML/CSS Preview

## Objective
Implement an iframe-based preview to render the generated UI for immediate visual feedback.

## Requirements
- Create a sandboxed iframe for rendering.
- Support automatic reloading when files change.
- Provide basic controls (e.1. resize, refresh).

## Deliverables
- Integration of the preview pane within the `BuilderResultPanel`.
